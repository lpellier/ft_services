apiVersion: v1
kind: Service
metadata:
 name: grafana
 annotations:
  metallb.universe.tf/allow-shared-ip: kub-ip 
 labels:
  app: services
  svc: grafana
spec:
 type: LoadBalancer
 selector:
  app: services
  svc: grafana
 ports:
  - name: grafana-port
    protocol: TCP
    port: 3000 
