apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: services
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      nodePort: 30080
    - name: https
      port: 443
      protocol: TCP
      nodePort: 30443
  type: LoadBalancer
  selector:
    run: nginx